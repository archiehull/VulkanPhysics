camera menu
list cameras drom config
mukti caners view in 4x4
each pannel hsve canera selection menu
add new camers + more controls

object placer
select object moves orbjt camers to object
click on object / crossbair on objext to select (pop up ckntect menu)
semected object glows / colour pulse
objrcter placer, defauly y10+ object hieght over 0,0
move object with wasdqe
different shape types (cylinder/nade/model)
connect / rotate
place ontop/next to
force object collisions in editor
group shapes
scale
parameters menu
- radius, xy linked (sq), xy, x2 length/width, scale, angle, shape type
drop object / throw object
object hotbar and hud
show trail/arc of throw to set params (2d tanks)

toggle live physics while editing

object spawner (drop ball every x seconds)

lightinf config snd placer (colourpicker)

set object orbit (around coord or other object)
add collision detectuon for path setting
-if collide then break off path !!
set object movement path (coord list) / speed
xy axis rotation
cuboid placer with mouse select disgonal coordinates
add fornite preset build models

set default scene
save scene

per object tezture menu/selection
procedural texuree menu
skybox selection

particle menu/emmiter on object
set emmitor params and attachment point(from top/bottom/centre etc.

hud / throw animation hands

step back/record movement to file
replay editior
latency calculation
per component latency view

full ecs

pop out / pin menu items as textboz

imgui scaling options

toggleable depth/normsl map (per-object)

highlight reel world/animation showing off grapbics / shaders

text boxes for info

saved animations/clips in replay menu
enable same seed or random see for physics

marble run / plink board (drop coin in shute bouncing off pegs(in tunnels))

roulette wheel -> betting mechanics
mini casino
poker chips on table, table shake

send force / wind with mouse
rubber band flick
click pushes forwaed in direction of camera
push size and strength params

input manager with easy key remapss 
edit/set on spot with context menu or keypress
inputs window (not menu cos too long)

windowed version of menu with extra properties

